# Location-based scheduler with Bryntum Scheduler Pro, TanStack Table, Shadcn UI, and Mapbox


Install the dependencies using the following command:

```sh
npm install
```

Run the development server using the following command:

```sh
npm run dev
```